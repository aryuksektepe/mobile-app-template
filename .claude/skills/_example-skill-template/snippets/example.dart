// Example snippet — paste-ready (in a real skill, complete + runnable).
//
// In real skills:
// - Imports at top
// - Following architecture.md contracts (Result<T, Failure>, sealed classes)
// - No placeholders without explicit `// TODO: replace with X` comments
// - Each file is a complete, runnable snippet (or close to it)

import 'package:flutter/widgets.dart';

class ExampleWidget extends StatelessWidget {
  const ExampleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
